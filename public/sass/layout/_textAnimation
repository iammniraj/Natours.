.typing {
  /* height: 80vh;  */
  display: flex;
  align-items: center;
  justify-content: center;

  &-effect {
    width: 22ch;
    animation: typing 2s steps(22),
      effect 0.5s step-start infinite alternate-reverse;
    white-space: nowrap;
    overflow: hidden;
    border-right: 3px solid;
    font-family: monospace;
    font-size: 1em;
  }
}

@keyframes typing {
  from {
    width: 0;
  }
}

@keyframes effect {
  50% {
    border-color: transparent;
  }
}
